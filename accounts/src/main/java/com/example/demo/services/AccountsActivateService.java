package com.example.demo.services;

public class AccountsActivateService {
    /*
    Search for email and token.

    Check if an email & token exist in the database.

    If they exist and the account is deactivated, activate the account.

    If they exist and the account is already activated, notify the user and ask them to change their password.

    Delete all tokens associated with the email.

    If no email & token exist, return a 404.
    */
}
